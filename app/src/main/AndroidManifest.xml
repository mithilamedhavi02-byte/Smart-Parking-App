<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SP"
        tools:targetApi="31">
        <activity
            android:name=".DriverLogin"
            android:exported="false" />
        <!-- Add Parking Activity -->
        <activity
            android:name=".Add_Parking"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize" /> <!-- Firebase Helper Activity (usually helper class, not activity) -->
        <activity
            android:name=".FirebaseDatabaseHelper"
            android:exported="false" /> <!-- Driver Register -->
        <activity
            android:name=".DriverRegister"
            android:exported="false" /> <!-- Main / Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Welcome Activity -->
        <activity
            android:name=".Welcome"
            android:exported="false" /> <!-- Admin Register Activity -->
        <activity
            android:name=".AdminRegister"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize" /> <!-- Admin Login Activity -->
        <activity
            android:name=".AdminLogin"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>